# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27

# Async database
sqlalchemy>=2.0
asyncpg==0.30.0
alembic==1.14.0

# Validation and settings
pydantic>=2.7,<3.0
pydantic-settings==2.7.0
email-validator==2.2.0

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==44.0.0

# Redis (rate limiting, token blacklisting, distributed locks)
redis[asyncio]>=5.0

# Async HTTP / OAuth / external APIs
httpx==0.28.1
authlib==1.3.2
itsdangerous==2.2.0

# Payments
stripe>=11.0,<13.0

# Task scheduling
APScheduler==3.10.4


# Ollama local LLM client
ollama>=0.3.0

# LLM providers (optional — only the chosen provider's SDK is needed)
openai>=1.30.0
google-generativeai>=0.7.0

# Multipart/form support
python-multipart==0.0.20

# File parsing (for chatbot file upload)
pypdf>=4.0.0
python-docx>=1.1.0

# PDF report rendering
jinja2>=3.1.0
playwright>=1.48.0

# Date parsing
python-dateutil==2.9.0.post0

# Environment variables
python-dotenv==1.0.1

# Testing async APIs
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-anyio==0.0.0
aiosqlite==0.20.0

# Development helpers
black==24.10.0
ruff==0.8.4